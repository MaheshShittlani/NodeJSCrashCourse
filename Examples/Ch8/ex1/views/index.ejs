<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>
<body>
    <header>
        <div id="logo">
            <h1>Idiots Tweek</h1>
            <p>An Idiotsdiary blog</p>
        </div>
        <%- include('./partials/nav') %>
    </header>
    <main>
        <div class="posts">
            <h2>Recent Updates</h2>
            <%
                if(blogs.length > 0 ) {
                    blogs.forEach(blog => {
                        %>
                        <div class="post">
                            <h3><%= blog.title %></h3>
                            <p><%= blog.snippet %> ...</p>
                        </div>
                        <%
                    })
                }else {
                    %>
                    <p>No blogs avaiable to display...</p>
                    <%
                }
            %>
        </div>
    </main>
</body>
</html>